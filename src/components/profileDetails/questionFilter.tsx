import React, { FunctionComponent } from 'react'
import { Paper } from '@material-ui/core'
type Props = {
  questions: any
}
export const QuestionFilter: FunctionComponent<Props> = ({
  questions,
}: Props) => {
  return <Paper>Questions</Paper>
}
